name = "lug"
version = "1.0.0"

description = "An implementation of the Lua VM"
licences = ["Apache-2.0"]
repository = { type = "github", user = "selenil", repo = "lug" }

[dependencies]
gleam_stdlib = ">= 0.44.0 and < 2.0.0"

[dev-dependencies]
gleeunit = ">= 1.0.0 and < 2.0.0"
